pipeline:
  docker-build:
    image: plugins/docker
    repo: docker.faavne.no/bzone/bztools
    registry: docker.faavne.no
    dockerfile: Dockerfile.bztools
    secrets: [ docker_username, docker_password ]
    tags: 
      - latest
      - ${DRONE_TAG}
    when:
      event:
        - tag
